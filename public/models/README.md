Place model files here.

Expected files:
- `anya.glb` (Anya character model)
- `ayrin.glb` (Ayrin character model)

To add a model locally, copy the .glb file into this folder and commit:

    git add public/models/anya.glb
    git commit -m "chore: add model anya.glb"
    git push

If you want me to add a file from a local path on this machine, provide the path or upload the file and I'll place it here and push.